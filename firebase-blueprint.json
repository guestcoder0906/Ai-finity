{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile and linked identity data",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "username": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["uid", "email"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "entity": "UserProfile",
      "description": "User profile document stored under their authenticated Firebase UID"
    }
  }
}
